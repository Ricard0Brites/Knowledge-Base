---
# Index Buffer View (IBV)

An index buffer holds integer indexes (offsets) into a vertex buffer, and is used to define and render an object made up of a subset of the full list of vertices.

An Index Buffer View (IBV) describes the structure and location of the index buffer.