---
# Unordered Access View (UAV)

An unordered access view provides similar functionality to a [[Shader Resource View (SRV)]], but enables reading and writing to the resource (like a texture) in any order from the shader.